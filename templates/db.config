#!/bin/bash

# TODO: DRY: FabFile.py 

DATASET=example
DB_README="ftp://ftp.ncbi.nlm.nih.gov/README.ftp"

DB_USER=`whoami`
DB_PASS=$SECRET
DB_HOST="localhost" 
DB_PORT="3306"  

mysql_dataset="mysql -D $DATASET -u $DB_USER -p$DB_PASS -h $DB_HOST --port $DB_PORT --local-infile" 
