DATASET=hugo
README="http://www.genenames.org/cgi-bin/download"

DB_USER=medgen
DB_PASS=medgen
DB_HOST="localhost" 
DB_PORT="3306"

mysql_dataset="mysql -D $DATASET -u $DB_USER -p$DB_PASS -h $DB_HOST --port $DB_PORT --local-infile" 
